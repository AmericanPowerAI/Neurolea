name: Test Neurolea

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Test Neurolea Framework
      run: |
        echo "🧠 Testing Neurolea Framework"
        python examples/quick_start.py
        echo "✅ Test completed!"
